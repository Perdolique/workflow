{
  "skill_name": "code-review",
  "evals": [
    {
      "id": 1,
      "prompt": "Perform only Step 3 of the code-review workflow from these already validated specialist reports. Behavioral specialist confirmed finding behavior-1: replacing an image deletes the current object before the replacement upload succeeds, so an upload failure loses the existing image; priority P1. Waste specialist confirmed finding waste-1: a one-variant wrapper enum adds unnecessary indirection and can be replaced directly without changing behavior; priority Low. Produce the final review report without re-reviewing code or spawning agents.",
      "expected_output": "The final report includes both confirmed findings, orders them by priority without filtering the Low cleanup, and reports zero omitted confirmed findings.",
      "files": [],
      "expectations": [
        "Step 3 performs no code inspection and spawns no agents because every input finding is already validated.",
        "The final findings include behavior-1 and its image-loss consequence.",
        "The final findings include waste-1 and its behavior-preserving cleanup even though it is Low priority.",
        "The final report does not describe waste-1 as cosmetic or non-material grounds for omission.",
        "The accounting reports two confirmed review findings, two final comments, zero duplicate merges, and zero omitted findings."
      ]
    },
    {
      "id": 2,
      "prompt": "Perform only Step 3 of the code-review workflow from these already validated specialist reports. Behavioral specialist confirmed behavior-1: an authorization check runs after the protected mutation. Security specialist confirmed security-1: the same mutation can occur before authorization. Waste specialist confirmed waste-1: a dead test-only counter is incremented but never read. Treat behavior-1 and security-1 as the same root cause. Produce the final review report without re-reviewing code or spawning agents.",
      "expected_output": "The final report merges the duplicate authorization findings with both source IDs, preserves the unique Low waste finding, and accounts for every confirmed ID exactly once.",
      "files": [],
      "expectations": [
        "Step 3 merges the duplicate IDs without inspecting code, rerunning validation, or spawning a resolver or validator.",
        "The final report contains one merged authorization finding rather than two duplicate comments.",
        "The merged authorization finding explicitly accounts for both behavior-1 and security-1.",
        "The final findings include waste-1 even though it is a Low cleanup.",
        "The accounting reports three confirmed review findings, two final comments, one duplicate merge, and zero omitted findings."
      ]
    },
    {
      "id": 3,
      "prompt": "Execute the specialist-finalization and Step 3 portions of the code-review workflow for one Waste and maintainability specialist. The exact scope is skills/code-review/evals/files/validator-fixture.ts. The specialist has two candidates: waste-1 says unusedLabel is dead because it has no references; waste-2 says formatTask is dead because it has no references. The candidates are not yet independently validated. Do not edit files. Use the skill's required validation workflow, then produce the final review report.",
      "expected_output": "The specialist uses one independent final finding validator, confirms waste-1 and the readability child's one-call indirection finding, rejects waste-2 because renderTask calls formatTask, and includes every confirmed cleanup with lossless accounting.",
      "files": [
        "evals/files/validator-fixture.ts"
      ],
      "expectations": [
        "The nonempty candidate package is checked by exactly one independent final finding validator.",
        "The final finding validator performs its internal assignment without invoking the code-review workflow or spawning children.",
        "The validator preserves both candidate IDs and confirms waste-1 with file evidence.",
        "The validator rejects waste-2 with the contradicting renderTask call as evidence.",
        "The validator also checks the readability child's supported one-call indirection candidate.",
        "The final findings include waste-1 and the confirmed readability cleanup, and do not present waste-2 as a finding.",
        "Step 3 spawns no agents after the validator returns its dispositions.",
        "The accounting reports two confirmed review findings, two final comments, zero duplicate merges, and zero omitted findings."
      ]
    },
    {
      "id": 4,
      "prompt": "Execute specialist finalization and Step 3 for a Behavioral and contract specialist that checked the supplied scope and returned no candidate findings. Do not edit files.",
      "expected_output": "The specialist returns checked scope and No issues found. without spawning a validator, and Step 3 reports no findings without spawning any agents.",
      "files": [],
      "expectations": [
        "The empty candidate package does not spawn a final finding validator or any other child.",
        "The specialist reports its checked scope and exactly No issues found.",
        "Step 3 performs no code inspection and spawns no agents."
      ]
    },
    {
      "id": 5,
      "prompt": "Continue the code-review workflow from one validated specialist report containing confirmed ID behavior-1 and unresolved ID behavior-2. The final validator already ran the smallest available reproduction for behavior-2 and reported that production-only state unavailable in the repository is still required. No new context or expertise is available. Produce Step 2 recovery and the final report without editing files.",
      "expected_output": "The workflow preserves behavior-1, reports behavior-2 as an incomplete review, and does not rerun the specialist assignment, reproduction, or validation because no new evidence can settle the unresolved ID.",
      "files": [],
      "expectations": [
        "The workflow does not spawn a retry specialist, resolver, reproducer, or validator when no new evidence can settle behavior-2.",
        "The completed reproduction for behavior-2 is not repeated.",
        "The confirmed behavior-1 disposition is not revisited and appears in the final findings.",
        "Behavior-2 remains separate as an incomplete review with the missing production-only context."
      ]
    },
    {
      "id": 6,
      "prompt": "Execute Waste and maintainability specialist finalization and Step 3 for a style-only scope containing `.card { min-width: 0; }`. The supplied layout evidence proves `.card` is a block in normal flow with no flex or grid constraint. Do not edit files.",
      "expected_output": "The parent partitions the style scope without reviewing its waste, one internal CSS child reports the ineffective declaration, one final validator checks the resulting candidate, and Step 3 performs no further review or agent work.",
      "files": [],
      "expectations": [
        "The Waste specialist identifies the style scope but does not independently review the min-width declaration.",
        "Exactly one CSS child owns the style waste review and reports the ineffective declaration with its layout evidence.",
        "The CSS child performs its internal assignment without invoking code-review orchestration or spawning children.",
        "Exactly one final finding validator checks the merged candidate list.",
        "Step 3 spawns no agents after validation."
      ]
    },
    {
      "id": 7,
      "prompt": "Continue the code-review workflow from one specialist report. Confirmed ID behavior-1 establishes that src/normalizeTitle.ts:1 declares const staleLabel = 'task' without references, so the declaration is dead source. Unresolved ID behavior-2 claims that src/normalizeTitle.ts:3 incorrectly preserves repeated interior spaces, which would violate canonical title normalization. The final validator already ran the smallest repository reproduction for behavior-2: normalizeTitle('  A  B  ') returned 'A  B'. A new authoritative contract is now available: normalizeTitle must remove leading and trailing whitespace while preserving interior whitespace exactly. Perform Step 2 recovery and Step 3 without editing files.",
      "expected_output": "The workflow gives only behavior-2, its exact prior reproduction result, and the new preservation contract to one fresh resolver; the resolver rejects behavior-2, behavior-1 remains confirmed, and the workflow does not rerun the specialist assignment or completed reproduction.",
      "files": [],
      "expectations": [
        "Exactly one fresh resolver receives only unresolved ID behavior-2, all prior evidence, and the new contract.",
        "The original specialist assignment and completed reproduction are not repeated.",
        "Confirmed ID behavior-1 is not revisited.",
        "The resolver rejects behavior-2 because the reproduced preservation of interior whitespace matches the authoritative contract.",
        "Step 3 consumes the resolver disposition without spawning another validator."
      ]
    },
    {
      "id": 8,
      "prompt": "Perform only Step 1 of the code-review workflow. The review target contains two uncommitted TypeScript files: src/normalizeTitle.ts changes a private pure helper to trim a title before returning it, and src/normalizeTitle.test.ts adds the matching focused assertion. The change has one local responsibility, no I/O or mutable state, no authorization, privacy, data-loss, migration, transaction, concurrency, public API, backward-compatibility, infrastructure, style, dependency, or cross-system behavior, and all required context is supplied. Eight lines changed and the focused test passed. Select the review mode and return planning output only.",
      "expected_output": "The planning agent selects compact mode once in shared context, returns one scope-only assignment packet plus separate reviewer routing and Available validation evidence, and does not spawn reviewers or report findings.",
      "files": [],
      "expectations": [
        "The planning agent selects exactly compact mode because the target has one coherent local responsibility and all applicable concerns fit one direct review assignment.",
        "The selection rationale does not treat the small file or line count as sufficient evidence by itself.",
        "The shared context declares compact mode once and records the changed behavior, entry points, and the focused test under Available validation evidence.",
        "Exactly one assignment packet covers both changed files and the title-normalization responsibility without repeating mode or first-level lifecycle instructions such as internal or direct-execution restrictions.",
        "Separate reviewer routing maps the packet to every applicable review role and the known applicable TypeScript domain skill.",
        "The planning agent does not spawn reviewers or report findings and guesses."
      ]
    },
    {
      "id": 9,
      "prompt": "Perform only Step 1 of the code-review workflow. The review target changes one line in src/deleteAccount.ts so the authorization check now runs after the account deletion. No other files changed and the diff is otherwise fully supplied. Select the review mode and return planning output only.",
      "expected_output": "The planning agent selects specialist mode once in shared context despite the tiny diff, returns focused scope-only packets with separate reviewer routing, and does not report findings.",
      "files": [],
      "expectations": [
        "The planning agent selects exactly specialist mode rather than compact mode.",
        "The selection rationale identifies authorization, mutation-before-permission, and data-loss risk instead of classifying by line count.",
        "The shared context declares specialist mode once rather than repeating it inside each assignment packet.",
        "Separate reviewer routing maps focused packets to Behavioral and contract, Data and concurrency, Security and vulnerability, and Test and regressions responsibilities.",
        "Each assignment packet contains only its coherent responsibility and relevant code or context, without packet-level mode, internal markers, or direct-execution restrictions.",
        "The planning agent does not spawn reviewers or present the supplied risk as a validated finding."
      ]
    },
    {
      "id": 10,
      "prompt": "Continue the code-review workflow from a completed Step 1 result that selected compact mode once in shared context. One scope-only packet covers src/normalizeTitle.ts with exact code `export function normalizeTitle(title: string): string { const normalizedTitle = title.trim(); return normalizedTitle; }`. Its focused test passes input `' task '` to normalizeTitle and asserts that the returned value is `'task'`; the test passed. Separate reviewer routing assigns Behavioral and contract, Test and regressions, and Waste and maintainability roles plus the TypeScript domain skill. Direct inspection establishes the intended trimming behavior and leaves no candidates. Execute Step 2 and Step 3 without editing files.",
      "expected_output": "The orchestrator builds one compact launch envelope from shared context, routing, and the scope-only packet; exactly one reviewer performs every assigned role directly without children; no validator is spawned for the empty candidate list; and the final report says No issues found.",
      "files": [],
      "expectations": [
        "Exactly one fresh compact reviewer receives a launch envelope containing the shared context, scope-only packet, compact reviewer instructions, every assigned review role, and the TypeScript domain skill.",
        "The launch envelope does not turn the packet itself into a container for mode, role, skill, or lifecycle routing metadata.",
        "The compact reviewer performs the full assignment directly without spawning specialist, readability, CSS, or validator children.",
        "The empty candidate list does not spawn a final finding validator.",
        "The final report identifies compact mode, reports checked scope and No issues found., and spawns no agents during Step 3."
      ]
    },
    {
      "id": 11,
      "prompt": "Continue the code-review workflow from a completed Step 1 result that selected compact mode once in shared context. One scope-only packet covers src/normalizeTitle.ts with exact code `const staleLabel = 'task'; export function normalizeTitle(title: string): string { const normalizedTitle = title.trim(); return normalizedTitle; }` and its focused test. Separate reviewer routing assigns Behavioral and contract, Test and regressions, and Waste and maintainability roles plus the TypeScript domain skill. The compact reviewer establishes that staleLabel has no references and the helper behavior is correct. Execute Step 2 and Step 3 without editing files.",
      "expected_output": "One compact reviewer directly returns a stable dead-code candidate without children, the orchestrator sends it to exactly one independent final validator, and the validated finding reaches the final lossless ledger.",
      "files": [],
      "expectations": [
        "Exactly one fresh compact reviewer receives the launch envelope, performs every assigned role directly, and spawns no children or validators.",
        "The compact reviewer assigns a stable ID to the staleLabel dead-code candidate and returns its exact evidence without confirming it itself.",
        "The orchestrator spawns exactly one fresh final finding validator with the candidate ID, packet code scope, prior evidence, applicable repository instructions and domain skills, and validator instructions rather than reusing the reviewer launch envelope.",
        "The validator confirms the staleLabel candidate from its lack of references, and Step 3 reports it once with zero duplicate merges and zero omitted findings.",
        "The finding does not infer compiler, build, deployment, or runtime effects without supplied evidence.",
        "Step 3 spawns no agents after receiving the validator disposition."
      ]
    },
    {
      "id": 12,
      "prompt": "Perform only Step 1 of the code-review workflow. Review range ec6a36342918c444ef2c29a6437b13b9ec8d163d...9af36ce5bbbfd0ce899c742a4f4c32db34f6ff3a adds an authenticated GET /api/catalog/search endpoint in a Cloudflare Worker, extracts shared current-session resolution from apps/api/src/auth/routes.ts into apps/api/src/auth/current-session.ts, adds locale-aware matching in apps/api/src/catalog/search.ts and PostgreSQL access in apps/api/src/catalog/repository.ts, introduces catalog tables in packages/database/src/schema.ts plus a generated migration and seeded data, and adds unit, database integration, and Worker integration tests. The Worker queries PostgreSQL through the existing Hyperdrive-backed database layer. CI on exact HEAD 9af36ce5bbbfd0ce899c742a4f4c32db34f6ff3a reports successful lint, typecheck, unit, integration, database migration, build, and staging jobs; skipped duplicate jobs are identified separately. No frontend or style files changed. Select the review mode and return planning output only without reviewing findings.",
      "expected_output": "The planning agent selects specialist mode once in shared context, records exact Available validation evidence once, returns coherent scope-only packets with separate reviewer routing, and does not leak child lifecycle instructions into first-level packets.",
      "files": [],
      "expectations": [
        "The planning agent selects specialist mode because authorization, a public API, a migration, seeded data, and Worker-to-database behavior cross material responsibility boundaries rather than because the diff is large.",
        "The shared context declares specialist mode once and records the exact target, changed contracts, entry points, and successful and skipped jobs under Available validation evidence without treating success as proof of correctness.",
        "Focused packets collectively cover every applicable Behavioral and contract, Data and concurrency, Security and vulnerability, Privacy and data protection, Performance and resource usage, Test and regressions, and Waste and maintainability responsibility while omitting Frontend and user experience.",
        "Separate reviewer routing maps every packet to its assigned review roles and known applicable domain skills rather than embedding that routing metadata in the packet.",
        "Each first-level assignment packet contains only a coherent responsibility and relevant code or context; it does not repeat mode or include internal markers or direct-execution restrictions intended for specialist-owned children and validators.",
        "The planning agent does not spawn reviewers, validate candidate findings, or report findings and guesses."
      ]
    },
    {
      "id": 13,
      "prompt": "Check the current uncommitted changes for problems before I commit. Look for bugs, regressions, and risky edge cases. Do not edit files.",
      "expected_output": "The target agent implicitly selects the code-review skill and performs its read-only review workflow because the request asks for an open-ended search for problems in a change set.",
      "files": [],
      "expectations": [
        "Automatic selection uses the target agent's real skill discovery mechanism without supplying or naming the code-review skill.",
        "The target agent selects the code-review skill despite the prompt using check instead of review.",
        "The agent treats all current uncommitted changes as the review target and keeps the work read-only."
      ]
    },
    {
      "id": 14,
      "prompt": "Check the current uncommitted changes by running lint, type checking, and tests. Report whether each command passes. Do not inspect the code for additional problems or edit files.",
      "expected_output": "The target agent runs the named validation commands and reports their results without selecting the code-review skill because the request is limited to specific checks.",
      "files": [],
      "expectations": [
        "Automatic selection uses the target agent's real skill discovery mechanism without supplying or naming the code-review skill.",
        "The target agent does not select the code-review skill or start its review orchestration.",
        "The agent runs only the relevant named checks and reports their actual results."
      ]
    },
    {
      "id": 15,
      "prompt": "Look over the current pull request and find defects, regressions, security risks, or maintainability problems. Do not edit files.",
      "expected_output": "The target agent implicitly selects the code-review skill and performs its read-only review workflow because the request asks for an open-ended review of a pull request.",
      "files": [],
      "expectations": [
        "Automatic selection uses the target agent's real skill discovery mechanism without supplying or naming the code-review skill.",
        "The target agent selects the code-review skill without requiring the exact phrase code review.",
        "The agent treats the current pull request as the review target and keeps the work read-only."
      ]
    },
    {
      "id": 16,
      "prompt": "Check these exact acceptance criteria in the current implementation: unauthenticated requests return 401 and valid sessions return 200. Use existing focused tests or direct code inspection only when needed. Report the evidence and do not edit files.",
      "expected_output": "The target agent verifies the two named requirements without selecting the code-review skill because the request does not ask for an open-ended search for issues.",
      "files": [],
      "expectations": [
        "Automatic selection uses the target agent's real skill discovery mechanism without supplying or naming the code-review skill.",
        "The target agent does not select the code-review skill or start its review orchestration.",
        "The agent limits its work to evidence for the two named requirements and reports the result."
      ]
    },
    {
      "id": 17,
      "prompt": "Perform only the internal Step 1 planning assignment for the changes in evals/files/conventions-fixture.md. Treat the snapshot as the complete review input. Return the review plan without spawning agents, reviewing findings, or editing files.",
      "expected_output": "The plan carries applicable global and repository guidance, the nested exception, and comparable formatter patterns with sources and scopes into shared context for the compact reviewer.",
      "files": [
        "evals/files/conventions-fixture.md"
      ],
      "expectations": [
        "Shared context preserves the global named-export rule, with its source and JavaScript scope.",
        "Shared context preserves the root formatter naming rule and the nested verbatim normalization exception, with their sources and scopes.",
        "The plan identifies normalizeLabel reuse in formatTitle and formatTag as comparable repository practice for formatBadge, without extending it to verbatim formatters.",
        "The plan selects compact mode and routes responsibility for applicable practices and consistency without creating a separate specialist.",
        "The planner does not return findings or spawn agents, and records passing output tests without inventing a lint result."
      ]
    },
    {
      "id": 18,
      "prompt": "Act as the internal compact reviewer for the changes in evals/files/conventions-fixture.md. Treat the snapshot as the complete review input. Your roles are Behavioral and contract, Test and regressions, and Waste and maintainability. Return candidates and checked scope directly without spawning agents or editing files.",
      "expected_output": "The reviewer identifies the global export and repository naming violations and the duplicated normalization pattern, while respecting the verbatim exception and excluding unsupported preferences.",
      "files": [
        "evals/files/conventions-fixture.md"
      ],
      "expectations": [
        "A candidate cites the default export in formatBadge and the applicable global named-export rule.",
        "A candidate cites fmtBadge and the root formatter naming rule even though the output tests pass.",
        "A candidate identifies the duplicated trim and Untitled normalization, cites normalizeLabel and comparable formatter callers, and explains the maintenance cost of a separate copy.",
        "Candidates have stable IDs and exact source locations, and do not invent a runtime or build failure for convention-only violations.",
        "No finding demands normalizeLabel in the verbatim formatter, uniform delimiters, or a particular quote style.",
        "The reviewer works directly, returns checked scope, and spawns no children or validators."
      ]
    },
    {
      "id": 19,
      "prompt": "Act as the internal final finding validator using evals/files/conventions-fixture.md. Validate only these candidates: export-1 says the default export in formatBadge violates the global named-export rule; naming-1 says fmtBadge violates the root formatter naming rule; reuse-1 says formatBadge duplicates normalization already shared by comparable formatters; verbatim-1 says formatCode must use normalizeLabel for consistency; quotes-1 says formatBadge must use single quotes because formatTitle does. Return one disposition per ID without spawning agents or editing files.",
      "expected_output": "The validator confirms the supported global, repository, and shared-helper findings without requiring runtime failures and rejects candidates contradicted by a scoped exception or lack of a quote rule.",
      "files": [
        "evals/files/conventions-fixture.md"
      ],
      "expectations": [
        "The validator confirms export-1 and naming-1 with applicable rule and code evidence rather than demanding a runtime failure.",
        "The validator confirms reuse-1 with the comparable helper use and duplicated normalization as evidence.",
        "The validator rejects verbatim-1 based on the nested verbatim exception.",
        "The validator rejects quotes-1 because quote style is not required and nearby examples alone do not establish that preference as a rule.",
        "Every supplied ID receives exactly one disposition; the validator adds no candidates, spawns no agents, and runs no unnecessary reproduction."
      ]
    },
    {
      "id": 20,
      "prompt": "Continue from a completed Step 1 plan: compact mode, one formatter-additions packet covering the two added files in evals/files/conventions-fixture.md, and Behavioral and contract plus Waste and maintainability roles. The fixture is the shared context, including applicable guidance, existing comparable code, and available validation evidence. Execute Step 2 and Step 3 without editing repository files. For this evaluation, save each exact child launch message and fork_turns value before dispatch, then record the returned agent ID and full response. Child agents must use only their launch message as review input, with no access to the source fixture or parent history. Use real child agents and keep artifacts in the supplied temporary output directory.",
      "expected_output": "A fresh compact reviewer receives the global and repository rules, nested exception, and comparable patterns. A separate fresh validator receives the guidance relevant to its candidate IDs. The final report includes only the three supported convention and reuse findings.",
      "files": [
        "evals/files/conventions-fixture.md"
      ],
      "expectations": [
        "The execution record identifies one real compact reviewer and one distinct final validator, both launched with fork_turns=\"none\".",
        "The recorded reviewer launch message carries the global named-export rule, root formatter naming rule, nested verbatim exception, and comparable normalization helper use with sources and scopes.",
        "The recorded validator launch message carries the global export and root naming rules with their scopes, the comparable normalization pattern, and exact reviewer candidate IDs rather than relying on a fixture path or inherited history.",
        "The final report preserves the export, naming, and helper-reuse findings and reports no quote-style or verbatim-normalization violation.",
        "The record shows no additional agents during Step 3, no child orchestration, and no repository edits."
      ]
    },
    {
      "id": 21,
      "prompt": "Continue Step 2 recovery and Step 3 using evals/files/conventions-fixture.md as shared context. A prior validator confirmed export-1 at src/formatBadge.js:1 for its forbidden default export and rejected quotes-1 because no quote style is required. It left verbatim-1 unresolved: the candidate demands normalizeLabel in src/verbatim/formatCode.js:2 based on formatTitle and formatTag. The completed reproduction returned \"<code>  A  </code>\" for formatCode(\"  A  \"). The nested src/verbatim/AGENTS.md in the shared context has now become available; it was absent from the prior evidence. Resolve the review without editing repository files. For this evaluation, save each exact child launch message and fork_turns value before dispatch, then record the returned agent ID and full response. Child agents must use only their launch message as review input, with no access to the source fixture or parent history. Use real child agents and keep artifacts in the supplied temporary output directory.",
      "expected_output": "One fresh resolver receives only the unresolved candidate, prior reproduction, and relevant guidance including the newly available scoped exception, rejects verbatim-1, and preserves the existing dispositions without repeating the reproduction.",
      "files": [
        "evals/files/conventions-fixture.md"
      ],
      "expectations": [
        "The execution record identifies exactly one fresh resolver launched with fork_turns=\"none\" and final finding validator instructions.",
        "The resolver launch message assigns only verbatim-1 and includes its exact prior reproduction result and comparable normalization pattern.",
        "The resolver launch message includes the nested verbatim rule, its source and directory scope, rather than only the candidate prior evidence or a fixture path.",
        "The resolver rejects verbatim-1 using the scoped exception; the final report preserves export-1 and does not revisit quotes-1.",
        "The record shows no repeated reproduction, restarted reviewer assignment, or additional validation agent."
      ]
    },
    {
      "id": 22,
      "prompt": "Perform Step 3 using evals/files/conventions-fixture.md as shared context and these validated reports. Both reports agree that export-1 at src/formatBadge.js:1 is confirmed because default exports are forbidden. For verbatim-1 at src/verbatim/formatCode.js:2, report A confirms that the formatter must use normalizeLabel because formatTitle and formatTag use it; report B rejects that candidate and says a directory-specific instruction permits the current code. The prior reproduction returned \"<code>  A  </code>\" for formatCode(\"  A  \"). Produce the final review report without editing repository files. For this evaluation, save each exact child launch message and fork_turns value before dispatch, then record the returned agent ID and full response. Child agents must use only their launch message as review input, with no access to the source fixture or parent history. Use real child agents and keep artifacts in the supplied temporary output directory.",
      "expected_output": "The conflict path sends only verbatim-1, both reports, the completed reproduction, and relevant scoped guidance to one fresh resolver, which rejects the unsupported finding before the final ledger is built.",
      "files": [
        "evals/files/conventions-fixture.md"
      ],
      "expectations": [
        "The execution record identifies exactly one fresh resolver launched with fork_turns=\"none\" under the Step 3 conflict path.",
        "The resolver launch message assigns only the conflicting verbatim-1 and includes both dispositions, prior reproduction, and final finding validator instructions.",
        "The resolver launch message includes the relevant comparable pattern and nested verbatim rule with their sources and scopes, without depending on parent history or fixture access.",
        "The resolver rejects verbatim-1 from the scoped exception; the final report retains export-1 exactly once with zero omitted confirmed findings.",
        "The record shows no new code inspection by the orchestrator, repeated reproduction, or agent launches after the conflict resolver completes."
      ]
    }
  ]
}
