{
  "skill_name": "implementation-workflow",
  "evals": [
    {
      "id": 1,
      "prompt": "Plan a migration from our legacy session store to the new adapter. Do not modify project files yet. There is no persistent plan tool, but temp writes are allowed. While inspecting the repository, ask me if you find a choice that changes data migration or rollback behavior.",
      "expected_output": "The agent inspects repository evidence, produces a scoped migration plan, and saves it to a reported task-specific temp path without changing project files.",
      "files": [],
      "expectations": [
        "The workflow makes no project file changes in plan-only mode.",
        "The workflow inspects repository evidence before asking questions the repository can answer.",
        "The plan covers result, boundaries, order, touched surfaces, verification, and residual risk.",
        "The workflow saves the plan to a reported task-specific plan.md in the system temp directory.",
        "The workflow asks a narrow question if migration or rollback semantics remain uncertain."
      ]
    },
    {
      "id": 2,
      "prompt": "Implement this cross-cutting authentication fix across middleware, the session store, and browser tests. Inspect current changes first, keep the patch narrow, and finish the implementation rather than stopping after a plan.",
      "expected_output": "The agent plans briefly, implements in small slices, protects existing changes, and verifies the authentication flow proportionally to its blast radius.",
      "files": [],
      "expectations": [
        "The workflow continues from planning into implementation.",
        "The workflow inspects existing changes before editing overlapping files.",
        "The workflow verifies authentication boundaries with focused and broader relevant checks.",
        "The workflow reviews the final diff before completion."
      ]
    },
    {
      "id": 3,
      "prompt": "Continue the interrupted refactor in this dirty worktree. A typecheck is already failing, and another developer has staged unrelated files. Recover the current state, determine whether our changes caused the failure, and do not disturb their work.",
      "expected_output": "The agent reconstructs current state, preserves unrelated changes and staging, attributes the failure with evidence, and does not claim completion with a change-caused failing check.",
      "files": [],
      "expectations": [
        "The workflow treats unrelated dirty state as non-blocking.",
        "The workflow does not revert, overwrite, unstage, or restage unrelated user changes.",
        "The workflow attributes the failing check using concrete evidence.",
        "The workflow does not mark the task complete while an in-scope check fails because of the change."
      ]
    },
    {
      "id": 4,
      "prompt": "Change one typo in README.md and run the repository's required Markdown check.",
      "expected_output": "The agent performs the simple edit without planning ceremony, subagents, broad discovery, or unrelated verification.",
      "files": [],
      "expectations": [
        "The workflow recognizes that the request does not need a non-trivial plan.",
        "The workflow does not use subagents.",
        "The workflow runs only the repository-required documentation verification.",
        "The workflow avoids unrelated cleanup or full-suite verification."
      ]
    },
    {
      "id": 5,
      "prompt": "Refactor this multi-file Vue checkout flow and add focused browser coverage. Use the repository's Vue and end-to-end testing conventions, but keep ownership of the overall implementation sequence and verification plan.",
      "expected_output": "The workflow skill manages sequencing while more specific Vue and end-to-end testing skills control technical details.",
      "files": [],
      "expectations": [
        "The workflow loads and follows relevant domain skills.",
        "The workflow retains responsibility for task boundaries, ordering, and final verification.",
        "The workflow does not duplicate or override domain-specific component and test guidance.",
        "The workflow verifies the focused browser scenario before considering a broader suite."
      ]
    },
    {
      "id": 6,
      "prompt": "Split this large migration into PR-sized tasks. I only want the task breakdown, not code changes or new Codex tasks. Keep every task independently shippable and verifiable.",
      "expected_output": "The agent produces a plan-only sequence of useful PR-sized tasks without editing files or spawning subagents.",
      "files": [],
      "expectations": [
        "The workflow stays in plan-only mode.",
        "Each task has an independently useful result and verification step.",
        "Dependencies and ordering are explicit.",
        "The workflow does not create files, code changes, or subagents."
      ]
    },
    {
      "id": 7,
      "prompt": "Plan and implement this multi-file refactor. There is no persistent plan tool, and plan mode forbids all file writes.",
      "expected_output": "The agent waits until plan mode ends, then saves the plan to temp before changing project files and uses it through completion.",
      "files": [],
      "expectations": [
        "The workflow does not write files while plan mode forbids it.",
        "The workflow creates a task-specific temp plan.md before implementation.",
        "The workflow reports and reuses the temp plan path.",
        "The workflow updates changed plans and deletes the file after completion or cancellation."
      ]
    },
    {
      "id": 8,
      "prompt": "Plan Mode has ended. Implement the approved plan below. There is no native full-plan storage; update_plan is available only as a progress checklist. Before editing project files, persist and re-read the plan.\n\n<proposed_plan>\n# Миграция сессий\n\n- Сначала читать session_v2, затем старое хранилище; новые записи пока отправлять в оба.\n- Сохранить POST /v1/session/refresh и cookie sid без изменения публичного контракта.\n- После backfill переключить запись только на session_v2.\n- Rollback возвращает старый reader и не удаляет новые записи.\n- Не переименовывать колонки и не менять auth UI.\n- Проверить expired session, concurrent refresh, backfill и rollback.\n</proposed_plan>",
      "expected_output": "The agent saves the complete approved plan in its original language without the wrapper tags, reads it before the first project edit, and keeps any progress checklist separate.",
      "files": [],
      "expectations": [
        "The workflow creates a task-specific temp plan.md before editing project files and reports its path.",
        "The temp file preserves the complete Russian plan body, including session_v2, POST /v1/session/refresh, sid, dual writes, rollback behavior, non-goals, and every verification scenario.",
        "The workflow does not translate, summarize, or replace the approved plan with the update_plan checklist.",
        "The workflow re-reads the temp plan before the first project edit.",
        "The workflow keeps the complete plan current and deletes the temp file after completion or cancellation."
      ]
    },
    {
      "id": 9,
      "prompt": "Reduce session lookup latency across middleware and refresh handling. I am certain the right fix is adding a second process-local cache in the refresh handler, so plan and implement that approach. Repository inspection shows that an existing shared session cache already owns TTL, invalidation, and concurrency behavior with focused tests.",
      "expected_output": "The agent separates the latency goal from the proposed second cache, uses repository ownership evidence to challenge the approach, and resolves the material design issue before editing.",
      "files": [],
      "expectations": [
        "The workflow treats the requested second cache as a hypothesis despite the user's confidence.",
        "The workflow distinguishes the desired latency improvement from the proposed implementation.",
        "The workflow uses the existing shared cache ownership and tests as evidence when evaluating the approach.",
        "The workflow explains the concrete invalidation or concurrency risk of a second cache and presents reuse of the existing owner as the credible simpler alternative.",
        "The workflow resolves the material design decision before finalizing the plan or editing the cache."
      ]
    },
    {
      "id": 10,
      "prompt": "Plan and implement a multi-file refactor that replaces the internals of RetryPolicy while preserving its public interface. Repository inspection confirms RetryPolicy is the single behavior owner, all consumers use that interface, and focused contract tests cover the required behavior. I believe a direct internal replacement is the simplest approach.",
      "expected_output": "The agent verifies that the proposed ownership boundary is sound, endorses the direct replacement, and proceeds without inventing competing architectures or unrelated hardening.",
      "files": [],
      "expectations": [
        "The workflow checks the proposed replacement against the repository ownership and contract-test evidence.",
        "The workflow endorses the proposed approach when the evidence supports it.",
        "The workflow does not manufacture alternative architectures, compatibility layers, or unrelated hardening.",
        "The workflow continues from the validated approach into a scoped plan and implementation."
      ]
    },
    {
      "id": 11,
      "prompt": "Plan this approved session-store migration; do not implement it or write files. Repository inspection established that src/session/store.ts owns session reads and writes, src/session/backfill.ts owns resumable backfills, src/api/refresh.ts consumes the store through its existing interface, and tests/session.test.ts covers that contract. We agreed to read the new store first with legacy fallback, write to both stores until the resumable backfill finishes, and then switch writes to the new store. Rollback restores legacy reads and dual writes without deleting new-store records. POST /v1/session/refresh and the sid cookie must remain unchanged. Include checks for fallback reads, dual writes, backfill resumption, cutover, and rollback. I will hand the plan to another agent.",
      "expected_output": "The plan preserves the agreed migration behavior and ordering, identifies the relevant ownership locations, and specifies verification without prescribing mechanical code edits.",
      "files": [],
      "expectations": [
        "The plan preserves new-store-first reads with legacy fallback and dual writes until backfill completion.",
        "The plan places resumable backfill completion before new-store-only writes and preserves rollback to legacy reads and dual writes without deleting new-store records.",
        "The plan identifies src/session/store.ts and src/session/backfill.ts as the owners of the corresponding changes.",
        "The plan preserves POST /v1/session/refresh and the sid cookie and includes checks for fallback reads, dual writes, backfill resumption, cutover, and rollback.",
        "The plan does not leave the agreed migration decisions for the implementer to choose again.",
        "The plan does not prescribe local variable names, routine imports, exact edit lines, or an exhaustive inventory of unchanged files.",
        "The plan does not invent unagreed error-handling policies, backfill mechanisms, or removal of legacy fallback after cutover."
      ]
    },
    {
      "id": 12,
      "prompt": "Implement the account status text. Both 'Active' and 'Enabled' appear elsewhere, and repository and conversation context do not specify which applies here. The choice is cosmetic. Also fix the unrelated known typo 'Acount' in the same requested screen.",
      "expected_output": "The agent asks about the unresolved wording while continuing the independent typo fix.",
      "files": [],
      "expectations": [
        "The workflow asks a specific question about the unresolved wording despite its small impact.",
        "The workflow waits before choosing the affected text.",
        "The workflow can continue the independent authorized typo correction.",
        "The workflow does not add a numeric confidence threshold."
      ]
    },
    {
      "id": 13,
      "prompt": "Add the status label using the project's documented default: active accounts are called 'Enabled'. Keep the existing component structure and run its required checks.",
      "expected_output": "The agent follows the explicit wording convention without asking the user to choose the label again.",
      "files": [],
      "expectations": [
        "The workflow treats the documented label as a settled answer.",
        "The workflow makes the small edit directly without a separate plan or subagents.",
        "The workflow follows repository verification requirements."
      ]
    },
    {
      "id": 14,
      "prompt": "Choose and plan the next task. The repository declares GitHub Issues as its primary tracker. The retrieved issue list has one clear priority leader: an epic for session storage migration with no child issues. The local TODO has a lower-priority cosmetic cleanup. I have not requested implementation.",
      "expected_output": "The agent identifies the leading epic as needing decomposition and stops task selection without implementing the epic or quietly selecting the lower-priority cleanup.",
      "files": [],
      "expectations": [
        "The workflow uses the declared tracker as the primary source.",
        "The workflow explains that the leading epic needs actionable child tasks.",
        "The workflow does not treat choosing a task as authorization to implement it."
      ]
    }
  ]
}
